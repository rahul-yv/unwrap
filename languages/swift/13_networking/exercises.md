# Exercises: Networking and APIs

1. Write a function `func echoOnce(port: UInt16, message: String) -> String` that connects to a listening socket on `port`, sends `message`, and returns what comes back.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
