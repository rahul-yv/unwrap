# Exercises: Conditionals

1. Write a function `func grade(_ score: Int) -> String` returning `"A"` for 90+, `"B"` for 80-89, `"C"` for 70-79, and `"F"` otherwise, using `switch` with range patterns.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
