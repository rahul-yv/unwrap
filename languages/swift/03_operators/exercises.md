# Exercises: Operators

1. Write a function `func clamp(_ value: Int, min: Int, max: Int) -> Int` that returns `value` clamped to the `[min, max]` range, using a closed range's `.contains`.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
