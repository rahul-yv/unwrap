# Exercises: Functions

1. Write a function `func makeCounter() -> () -> Int` returning a closure; each call to the returned closure returns an incrementing count starting at 1 (use a closure over a local `var`).

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
