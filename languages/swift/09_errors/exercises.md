# Exercises: Error Handling

1. Write a function `func safeParseInt(_ s: String) -> Result<Int, Error>` that parses `s` as an `Int`, returning `.success` or `.failure` with a custom error instead of crashing on invalid input.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
