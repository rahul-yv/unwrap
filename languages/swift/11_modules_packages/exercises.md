# Exercises: Modules and Packages

1. Using `mypackage/Helpers.swift`'s `greet(_:)`, write `func exampleUsage() -> String` in `solutions/main.swift` returning `greet("World")`.

Check your answer against [`solutions/main.swift`](./solutions/main.swift).
