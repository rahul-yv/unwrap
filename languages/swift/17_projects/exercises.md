# Exercises: Mini Project

1. Extend the counter into `func topWordsExcluding(path: String, n: Int, stopwords: Set<String>) throws -> [(word: String, count: Int)]` — same as `topWords`, but skipping any word present in `stopwords`.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
