# Exercises: Testing

1. Using the `TestRunner` pattern, write checks that `add(0, 0) == 0` and `add(-1, 1) == 0`, exiting with the runner's summary exit code.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
