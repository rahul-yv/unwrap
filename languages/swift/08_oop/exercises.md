# Exercises: Object-Oriented Programming

1. Write an `enum Shape` with cases `circle(radius: Double)` and `rectangle(width: Double, height: Double)`, and a function `func perimeter(_ shape: Shape) -> Double` covering both with `switch`.

Check your answer against [`solutions/exercise_1.swift`](./solutions/exercise_1.swift).
