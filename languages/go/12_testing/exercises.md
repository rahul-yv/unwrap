# Exercises: Testing

1. Write a table-driven `TestAdd` (using `t.Run` for named subtests) checking `Add(0, 0) == 0` and `Add(-1, 1) == 0`.

Check your answer against [`solutions/exercise_1_test.go`](./solutions/exercise_1_test.go).
