# Exercises: Collections

1. Write `wordCounts(words []string) map[string]int` returning a map from each word to its occurrence count.
   - `wordCounts([]string{"a", "b", "a"})` → `map[a:2 b:1]`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
