# Exercises: Operators

1. Write `divide(a, b int) (int, bool)` returning `a / b, true`, or `0, false` if `b == 0`, using comma-ok instead of panicking.
   - `divide(10, 2)` → `(5, true)`
   - `divide(10, 0)` → `(0, false)`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
