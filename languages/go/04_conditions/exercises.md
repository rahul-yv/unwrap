# Exercises: Conditionals

1. Write `grade(score int) string` returning `"A"` for `>= 90`, `"B"` for `>= 80`, `"C"` for `>= 70`, `"F"` otherwise, using a tag-less `switch`.
   - `grade(95)` → `"A"`
   - `grade(72)` → `"C"`
   - `grade(40)` → `"F"`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
