# Exercises: Modules and Packages

1. Using `mypackage/helpers.go`'s `Greet(name string) string`, write `ExampleUsage() string` that calls `mypackage.Greet("World")`.
   - `ExampleUsage()` → `"Hello, World!"`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
