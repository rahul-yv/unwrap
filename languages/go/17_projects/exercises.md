# Exercises: Mini Project

1. Write `topWordsExcluding(path string, n int, stopwords map[string]bool) ([]WordCount, error)` — same as `topWords`, excluding words present (with value `true`) in `stopwords`.

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
