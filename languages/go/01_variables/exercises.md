# Exercises: Variables

1. Write `swap(a, b int) (int, int)` returning `b, a` using Go's multi-value return, not a temp variable.
   - `swap(1, 2)` → `(2, 1)`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
