# Exercises: OOP

1. Define `Rectangle` with `Width, Height float64`.
2. Write `Area() float64` and `Equals(other Rectangle) bool` (compares `Width`/`Height`).
   - `Rectangle{3, 4}.Area()` → `12`
   - `Rectangle{3, 4}.Equals(Rectangle{3, 4})` → `true`

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
