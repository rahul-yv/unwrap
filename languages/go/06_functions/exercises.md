# Exercises: Functions

1. Write `makeCounter() func() int` returning a closure; each call to the returned function returns an incrementing count starting at 1.

Check your answer against [`solutions/exercise_1.go`](./solutions/exercise_1.go).
