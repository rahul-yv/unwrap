# Exercises: OOP

1. Write a `Rectangle` class with private `width`/`height` fields, a `.area()` method, and an `.equals(other)` method comparing by `(width, height)`.
   - `new Rectangle(3, 4).area()` → `12`
   - `new Rectangle(3, 4).equals(new Rectangle(3, 4))` → `true`
   - `new Rectangle(3, 4).equals(new Rectangle(4, 3))` → `false`

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
