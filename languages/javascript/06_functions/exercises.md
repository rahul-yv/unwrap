# Exercises: Functions

1. Write `makeCounter()` returning a zero-argument function; each call returns an incrementing count starting at 1.

   ```javascript
   const counter = makeCounter();
   counter(); // 1
   counter(); // 2
   counter(); // 3
   ```

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
