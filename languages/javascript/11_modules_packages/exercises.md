# Exercises: Modules and Packages

1. Using `mypackage/helpers.mjs`'s `greet(name)`, write `exampleUsage()` that imports `greet` by name and returns `greet("World")`.
   - `exampleUsage()` → `"Hello, World!"`

Check your answer against [`solutions/exercise_1.mjs`](./solutions/exercise_1.mjs).
