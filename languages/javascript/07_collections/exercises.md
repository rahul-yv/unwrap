# Exercises: Collections

1. Write `wordCounts(words)` returning a `Map` from each word to its occurrence count.
   - `wordCounts(["a", "b", "a"])` → `Map { "a" => 2, "b" => 1 }`

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
