# Exercises: Testing

1. Given `example.js`'s `add(a, b)`, write a `node:test` test checking `add(0, 0) === 0` and `add(-1, 1) === 0`.

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
