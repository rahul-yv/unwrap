# Exercises: Mini Project

1. Write `topWordsExcluding(filePath, n, stopwords)` — same as `topWords`, but words in `stopwords` (a `Set`) are excluded before counting.

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
