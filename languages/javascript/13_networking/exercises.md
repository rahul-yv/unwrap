# Exercises: Networking and APIs

1. Write an async function `fetchJson(url, options)` calling `fetch(url, options)`, throwing an `Error` with a clear message if `!response.ok`, otherwise returning the parsed JSON body.

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
