# Exercises: Loops

1. Write `firstEven(numbers)` returning the first even number, or `undefined` if none, using `for...of`.
   - `firstEven([1, 3, 4, 5])` → `4`
   - `firstEven([1, 3, 5])` → `undefined`

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
