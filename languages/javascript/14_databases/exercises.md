# Exercises: Databases

1. Write `getUserByName(db, name)` using a prepared, parameterized statement to look up a user in a `users(id, name)` table, returning the row or `undefined` if not found.

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
