# Exercises: Error Handling

1. Write `safeDivide(a, b)` returning `a / b`, or `null` if `b` is `0`. Throw an `Error` when `b === 0` and catch it locally, rather than short-circuiting with a plain `if`/`return null`.

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
