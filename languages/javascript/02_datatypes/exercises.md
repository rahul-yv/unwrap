# Exercises: Data Types

1. Write `describe(value)` returning `"number"`, `"string"`, `"boolean"`, `"null"`, or `"undefined"` — must correctly report `"null"` for `null` (not `"object"`).
   - `describe(5)` → `"number"`
   - `describe(null)` → `"null"`
   - `describe(undefined)` → `"undefined"`

Check your answer against [`solutions/exercise_1.js`](./solutions/exercise_1.js).
