# Exercises: Object-Oriented Programming

1. Write a `sealed interface Shape` with `data class` variants `Circle(radius: Double)`, `Rectangle(width: Double, height: Double)`, and a function `fun perimeter(shape: Shape): Double` covering both with `when` (no `else`).

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
