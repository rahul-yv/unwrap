# Exercises: Loops

1. Write a function `fun sumEvens(n: Int): Int` that returns the sum of all even numbers from `0` to `n` inclusive, using a range with `step`.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
