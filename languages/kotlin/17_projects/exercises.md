# Exercises: Mini Project

1. Extend the counter into `fun topWordsExcluding(path: String, n: Int, stopwords: Set<String>): List<Pair<String, Int>>` — same as `topWords`, but skipping any word present in `stopwords`.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
