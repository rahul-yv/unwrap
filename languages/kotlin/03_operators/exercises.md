# Exercises: Operators

1. Write a function `fun clamp(value: Int, min: Int, max: Int): Int` that returns `value` clamped to the `[min, max]` range, using `in` to check membership.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
