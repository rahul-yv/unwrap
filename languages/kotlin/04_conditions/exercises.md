# Exercises: Conditionals

1. Write a function `fun grade(score: Int): String` returning `"A"` for 90+, `"B"` for 80-89, `"C"` for 70-79, and `"F"` otherwise, using `when` with range conditions.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
