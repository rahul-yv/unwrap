# Exercises: Functions

1. Write a function `fun makeCounter(): () -> Int` returning a function; each call to the returned function returns an incrementing count starting at 1 (use a closure over a local `var`).

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
