# Exercises: Concurrency

1. Write a function `fun sumConcurrently(numbers: List<Int>): Int` that splits `numbers` into two halves, sums each half on its own `Thread`, and combines the results after joining both threads.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
