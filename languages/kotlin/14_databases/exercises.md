# Exercises: Databases

1. Write a function `fun getUserName(conn: java.sql.Connection, id: Long): String?` that returns the user's name for a given `id` via a parameterized query, or `null` if no row matches.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
