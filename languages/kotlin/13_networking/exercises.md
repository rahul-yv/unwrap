# Exercises: Networking and APIs

1. Write a function `fun echoOnce(port: Int, message: String): String` that connects to a `ServerSocket` already listening on `port`, sends `message`, and returns what comes back.

Check your answer against [`solutions/exercise_1.kt`](./solutions/exercise_1.kt).
