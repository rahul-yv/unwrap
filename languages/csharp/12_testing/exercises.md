# Exercises: Testing

1. Using the harness pattern in `example.cs`, write checks that `Add(0, 0) == 0` and `Add(-1, 1) == 0`, returning a nonzero exit code if any check fails.

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
