# Exercises: Conditionals

1. Write `string Grade(int score)` returning `"A"` for `score >= 90`, `"B"` for `>= 80`, `"C"` for `>= 70`, `"F"` otherwise, using a `switch` expression with relational patterns.
   - `Grade(95)` → `"A"`
   - `Grade(72)` → `"C"`
   - `Grade(40)` → `"F"`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
