# Exercises: Modules and Packages

1. Define a namespace `MyPackage` containing `public static class Greeter` with `Greet(string name)`, then write `example_usage()` returning `MyPackage.Greeter.Greet("World")`.
   - `example_usage()` → `"Hello, World!"`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
