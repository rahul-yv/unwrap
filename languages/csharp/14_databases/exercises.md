# Exercises: Databases

1. Write a method `string? GetUserName(SqliteConnection conn, long id)` that returns the user's name for a given `id` via a parameterized query, or `null` if no row matches.

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
