# Exercises: Data Types

1. Write `int Describe(int? value)` returning `value` doubled if it has a value, or `-1` if it's `null` (handle the nullable safely, no raw `.Value`).
   - `Describe(5)` → `10`
   - `Describe(null)` → `-1`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
