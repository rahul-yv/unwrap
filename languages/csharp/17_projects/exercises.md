# Exercises: Mini Project

1. Extend the counter into `List<(string Word, int Count)> TopWordsExcluding(string path, int n, IReadOnlySet<string> stopwords)` — same as `TopWords`, but skipping any word present in `stopwords`.

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
