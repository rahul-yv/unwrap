# Exercises: Files and I/O

1. Write `int CountLines(string path)` returning the number of lines in the file, or `-1` if it doesn't exist (use `File.Exists` and `File.ReadLines`).

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
