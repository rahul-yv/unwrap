# Exercises: Operators

1. Write `string DisplayName(string? name)` returning `name` if it's non-null and non-empty, otherwise `"Anonymous"`.
   - `DisplayName("Ada")` → `"Ada"`
   - `DisplayName(null)` → `"Anonymous"`
   - `DisplayName("")` → `"Anonymous"`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
