# Exercises: Collections

1. Write `Dictionary<string, int> WordCounts(string[] words)` returning a dictionary from each word to its occurrence count.
   - `WordCounts(new[] {"a", "b", "a"})` → `{"a": 2, "b": 1}`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
