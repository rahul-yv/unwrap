# Exercises: Loops

1. Write `int SumEven(int[] numbers)` returning the sum of the even numbers, using a `foreach` loop.
   - `SumEven(new[] {1, 2, 3, 4})` → `6`

Check your answer against [`solutions/exercise_1.cs`](./solutions/exercise_1.cs).
