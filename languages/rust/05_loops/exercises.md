# Exercises: Loops

1. Write `fn first_even(numbers: &[i32]) -> Option<i32>` returning the first even number, or `None`, using a `for` loop.
   - `first_even(&[1, 3, 4, 5])` → `Some(4)`
   - `first_even(&[1, 3, 5])` → `None`

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
