# Exercises: Operators

1. Write `fn clamp(value: i32, low: i32, high: i32) -> i32` restricting `value` to `[low, high]`.
   - `clamp(5, 0, 10)` → `5`
   - `clamp(-5, 0, 10)` → `0`
   - `clamp(15, 0, 10)` → `10`

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
