# Exercises: Mini Project

1. Write `fn top_words_excluding(path: &str, n: usize, stopwords: &[&str]) -> std::io::Result<Vec<(String, i32)>>` — same as `top_words`, excluding words present in `stopwords`.

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
