# Exercises: Conditionals

1. Define `enum TrafficLight { Red, Yellow, Green }`.
2. Write `fn action(light: &TrafficLight) -> &str` returning `"stop"`, `"slow down"`, `"go"` respectively, using `match`.

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
