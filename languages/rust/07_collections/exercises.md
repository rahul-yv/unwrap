# Exercises: Collections

1. Write `fn word_counts(words: &[&str]) -> HashMap<String, i32>` returning a map from each word to its occurrence count.
   - `word_counts(&["a", "b", "a"])` → `{"a": 2, "b": 1}`

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
