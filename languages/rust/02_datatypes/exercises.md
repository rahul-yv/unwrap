# Exercises: Data Types

1. Write `fn count_chars(s: &str) -> usize` returning the number of Unicode characters (not bytes) in `s`.
   - `count_chars("héllo")` → `5`
   - `count_chars("hello")` → `5`

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
