# Exercises: Testing

1. Given `example.rs`'s `add(a: i32, b: i32) -> i32`, write `#[test]` functions checking `add(0, 0) == 0` and `add(-1, 1) == 0`.

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
