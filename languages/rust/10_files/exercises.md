# Exercises: Files and I/O

1. Write `fn count_lines(path: &str) -> std::io::Result<usize>` returning the number of lines in the file, using `BufReader::lines()`.

Check your answer against [`solutions/exercise_1.rs`](./solutions/exercise_1.rs).
