# Exercises: Conditionals

1. Write `char grade(int score)` returning `'A'` for `score >= 90`, `'B'` for `>= 80`, `'C'` for `>= 70`, `'F'` otherwise.
   - `grade(95)` → `'A'`
   - `grade(72)` → `'C'`
   - `grade(40)` → `'F'`

Check your answer against [`solutions/exercise_1.c`](./solutions/exercise_1.c).
