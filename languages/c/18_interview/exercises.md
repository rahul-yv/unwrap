# Exercises: Interview Prep

1. Write `struct Node *reverse_list(struct Node *head)` reversing a singly linked list in place (iteratively, with three pointers: previous, current, next), returning the new head. The `struct Node { int value; struct Node *next; };` definition is in the solution file.

Check your answer against [`solutions/exercise_1.c`](./solutions/exercise_1.c).
