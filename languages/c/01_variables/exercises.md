# Exercises: Variables

1. Write `void swap(int *a, int *b)` that swaps the values pointed to by `a` and `b`.

   ```c
   int x = 1, y = 2;
   swap(&x, &y);
   // x == 2, y == 1
   ```

Check your answer against [`solutions/exercise_1.c`](./solutions/exercise_1.c).
