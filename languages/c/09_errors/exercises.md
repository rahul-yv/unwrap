# Exercises: Error Handling

1. Write `int safe_divide(int a, int b, int *result)` returning `0` and setting `*result = a / b` on success, or `-1` (leaving `*result` untouched) if `b == 0`.

Check your answer against [`solutions/exercise_1.c`](./solutions/exercise_1.c).
