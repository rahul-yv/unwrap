# Exercises: Mini Project

1. Add `int most_frequent(const WordCount *entries, int count)` returning the index of the entry with the highest count (first one wins ties), or `-1` if `count` is 0.

Check your answer against [`solutions/exercise_1.c`](./solutions/exercise_1.c).
