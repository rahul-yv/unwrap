# Exercises: Networking and APIs

1. Write a function `Future<String> echoOnce(int port, String message)` that connects to a listening socket on `port`, sends `message`, and returns what comes back.

Check your answer against [`solutions/exercise_1.dart`](./solutions/exercise_1.dart).
