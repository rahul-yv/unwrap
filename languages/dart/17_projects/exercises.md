# Exercises: Mini Project

1. Extend the counter into `List<MapEntry<String, int>> topWordsExcluding(String path, int n, Set<String> stopwords)` — same as `topWords`, but skipping any word present in `stopwords`.

Check your answer against [`solutions/exercise_1.dart`](./solutions/exercise_1.dart).
