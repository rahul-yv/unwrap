# Exercises: Error Handling

1. Write a function `int? safeParseInt(String s)` that parses `s` as an integer, returning `null` if it isn't a valid integer string (using `int.tryParse`, which returns `null` instead of throwing).

Check your answer against [`solutions/exercise_1.dart`](./solutions/exercise_1.dart).
