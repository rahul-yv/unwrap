# Exercises: Databases

1. Write a function `String? getUserName(Database db, int id)` that returns the user's name for a given `id` via a parameterized query, or `null` if no row matches.

Check your answer against [`solutions/exercise_1.dart`](./solutions/exercise_1.dart) (run with `dart run solutions/exercise_1.dart`).
