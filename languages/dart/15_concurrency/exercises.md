# Exercises: Concurrency

1. Write a function `Future<int> sumConcurrently(List<int> numbers)` that splits `numbers` into two halves and sums each half in its own `Isolate`, combining the results.

Check your answer against [`solutions/exercise_1.dart`](./solutions/exercise_1.dart).
