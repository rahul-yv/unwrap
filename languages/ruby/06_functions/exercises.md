# Exercises: Functions

1. Write a method `def make_counter` returning a lambda; each call to the returned lambda returns an incrementing count starting at 1 (use a closure over a local variable).

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
