# Exercises: Loops

1. Write a method `def sum_evens(n)` that returns the sum of all even numbers from `0` to `n` inclusive, using `.step` or a `Range` with `.select`.

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
