# Exercises: Concurrency

1. Write a method `def sum_concurrently(numbers)` that splits `numbers` into two halves, sums each half on its own `Thread`, and combines the results after joining both threads.

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
