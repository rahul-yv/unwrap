# Exercises: Conditionals

1. Write a method `def grade(score)` returning `"A"` for 90+, `"B"` for 80-89, `"C"` for 70-79, and `"F"` otherwise, using `case`/`when` with ranges.

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
