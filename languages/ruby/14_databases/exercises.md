# Exercises: Databases

1. Write a method `def get_user_name(db, id)` that returns the user's name for a given `id` via a parameterized query, or `nil` if no row matches.

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
