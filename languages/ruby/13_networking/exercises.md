# Exercises: Networking and APIs

1. Write a method `def echo_once(port, message)` that connects to a listening `TCPServer` on `port`, sends `message`, and returns what comes back.

Check your answer against [`solutions/exercise_1.rb`](./solutions/exercise_1.rb).
