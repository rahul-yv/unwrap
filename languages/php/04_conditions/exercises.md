# Exercises: Conditionals

1. Write a function `function grade(int $score): string` returning `"A"` for 90+, `"B"` for 80-89, `"C"` for 70-79, and `"F"` otherwise, using `match (true)`.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
