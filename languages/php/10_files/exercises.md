# Exercises: Files and I/O

1. Write a function `function countLines(string $path): int` that returns the number of lines in the file at `$path`, using `fopen`/`fgets` (not reading the whole file into an array first).

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
