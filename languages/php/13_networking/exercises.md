# Exercises: Networking and APIs

1. Write a function `function echoOnce(int $port, string $message): string` that connects to a listening socket on `$port`, sends `$message`, and returns what comes back.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
