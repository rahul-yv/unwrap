# Exercises: Mini Project

1. Extend the counter into `function topWordsExcluding(string $path, int $n, array $stopwords): array` — same as `topWords`, but skipping any word present in `$stopwords`.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
