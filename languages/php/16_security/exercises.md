# Exercises: Security Basics

1. Write `function hashPassword(string $password): string` using `password_hash()`, and `function verifyPassword(string $password, string $hash): bool` using `password_verify()`.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
