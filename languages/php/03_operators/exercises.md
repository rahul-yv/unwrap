# Exercises: Operators

1. Write a function `function clamp(int $value, int $min, int $max): int` that returns `$value` clamped to the `[$min, $max]` range.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
