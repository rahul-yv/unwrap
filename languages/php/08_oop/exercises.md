# Exercises: Object-Oriented Programming

1. Write an `enum Shape` (backed by `string`) with cases `Circle` and `Square`, and a function `function describe(Shape $shape): string` using `match` to return a description for each case.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
