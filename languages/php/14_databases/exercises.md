# Exercises: Databases

1. Write a function `function getUserName(PDO $pdo, int $id): ?string` that returns the user's name for a given `id` via a parameterized query, or `null` if no row matches.

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
