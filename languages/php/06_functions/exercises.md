# Exercises: Functions

1. Write a function `function makeCounter(): callable` returning a closure; each call to the returned closure returns an incrementing count starting at 1 (use `use (&$count)`).

Check your answer against [`solutions/exercise_1.php`](./solutions/exercise_1.php).
