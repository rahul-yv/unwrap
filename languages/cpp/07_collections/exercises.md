# Exercises: Collections

1. Write `std::unordered_map<std::string, int> word_counts(const std::vector<std::string>& words)` returning a map from each word to its occurrence count.
   - `word_counts({"a", "b", "a"})` → `{{"a", 2}, {"b", 1}}`

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
