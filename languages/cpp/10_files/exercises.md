# Exercises: Files and I/O

1. Write `int count_lines(const std::string& path)` returning the number of lines, or `-1` if the file can't be opened, using `std::ifstream` and `std::getline`.

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
