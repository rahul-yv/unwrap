# Exercises: Modules and Packages

1. Using `mypackage.hpp`/`mypackage.cpp`'s `mypackage::greet`, write `std::string example_usage()` returning `mypackage::greet("World")`.
   - `example_usage()` → `"Hello, World!"`

Compile with `c++ solutions/exercise_1.cpp mypackage.cpp -o exercise_1`.

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
