# Exercises: Mini Project

1. Add `std::optional<std::pair<std::string, int>> most_frequent(const std::unordered_map<std::string, int>& counts)` returning the most frequent word and its count, or `std::nullopt` if the map is empty.

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
