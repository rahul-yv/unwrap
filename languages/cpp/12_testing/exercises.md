# Exercises: Testing

1. Using the `TestRunner` pattern, write checks that `add(0, 0) == 0` and `add(-1, 1) == 0`, returning the runner's summary exit code from `main`.

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
