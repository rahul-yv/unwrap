# Exercises: Loops

1. Write `int sum_even(const std::vector<int>& numbers)` returning the sum of the even numbers, using a range-based for loop with `const&`.
   - `sum_even({1, 2, 3, 4})` → `6`

Check your answer against [`solutions/exercise_1.cpp`](./solutions/exercise_1.cpp).
