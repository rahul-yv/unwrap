# Exercises: Loops

1. Write `first_even(numbers)` that returns the first even number in the list, or `None` if there isn't one. Use `for`/`else`.
   - `first_even([1, 3, 4, 5])` → `4`
   - `first_even([1, 3, 5])` → `None`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
