# Exercises: Operators

1. Write `clamp(value, low, high)` that restricts `value` to `[low, high]`, using `max`/`min` instead of an `if`/`else` chain.
   - `clamp(5, 0, 10)` → `5`
   - `clamp(-5, 0, 10)` → `0`
   - `clamp(15, 0, 10)` → `10`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
