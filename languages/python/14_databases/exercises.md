# Exercises: Databases

1. Write `get_user_by_name(conn, name)` that queries a `users(id, name)` table with a parameterized query and returns `{"id": ..., "name": ...}`, or `None` if no row matches.

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
