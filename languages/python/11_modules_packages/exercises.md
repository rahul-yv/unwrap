# Exercises: Modules and Packages

1. Using `mypackage/helpers.py`'s `greet(name)` function, write `example_usage()` that imports `greet` by name (not `import *`) and returns `greet("World")`.
   - `example_usage()` → `"Hello, World!"`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
