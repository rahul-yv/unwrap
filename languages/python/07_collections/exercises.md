# Exercises: Collections

1. Write `word_counts(words)` returning a dict mapping each word to its occurrence count. Use a dict comprehension or `collections.Counter`, not a manual loop with membership checks.
   - `word_counts(["a", "b", "a"])` → `{"a": 2, "b": 1}`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
