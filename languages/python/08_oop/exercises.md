# Exercises: OOP

1. Write a `Rectangle` class with `width` and `height`, an `.area()` method, and an `__eq__` that compares two rectangles by `(width, height)`.
   - `Rectangle(3, 4).area()` → `12`
   - `Rectangle(3, 4) == Rectangle(3, 4)` → `True`
   - `Rectangle(3, 4) == Rectangle(4, 3)` → `False`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
