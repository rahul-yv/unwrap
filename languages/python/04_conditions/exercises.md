# Exercises: Conditionals

1. Write `grade(score)` that returns `"A"` for `score >= 90`, `"B"` for `score >= 80`, `"C"` for `score >= 70`, and `"F"` otherwise.
   - `grade(95)` → `"A"`
   - `grade(72)` → `"C"`
   - `grade(40)` → `"F"`

Check your answer against [`solutions/exercise_1.py`](./solutions/exercise_1.py).
