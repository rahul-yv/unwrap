# Exercises: Functions

1. Write `firstOrDefault<T>(items: T[], defaultValue: T): T` returning the first element, or `defaultValue` if `items` is empty.
   - `firstOrDefault([1, 2, 3], 0)` → `1`
   - `firstOrDefault([], 0)` → `0`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
