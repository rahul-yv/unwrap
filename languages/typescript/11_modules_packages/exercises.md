# Exercises: Modules and Packages

1. Using `mypackage/helpers.ts`'s `greet(name: string): Greeting`, write `exampleUsage(): string` importing `greet` (and `Greeting` via `import type`) and returning `greet("World").message`.
   - `exampleUsage()` → `"Hello, World!"`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
