# Exercises: Mini Project

1. Write `topWordsExcluding(filePath: string, n: number, stopwords: Set<string>): Promise<Array<[string, number]>>` — same as `topWords`, excluding words in `stopwords`.

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
