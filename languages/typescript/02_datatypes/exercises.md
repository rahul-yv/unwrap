# Exercises: Data Types

1. Define an interface `Point` with `x: number` and `y: number`.
2. Write `distance(a: Point, b: Point): number` computing the Euclidean distance between them.
   - `distance({x: 0, y: 0}, {x: 3, y: 4})` → `5`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
