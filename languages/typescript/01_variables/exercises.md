# Exercises: Variables

1. Write `swap<T>(pair: [T, T]): [T, T]` returning the pair reversed, generic over `T`.
   - `swap([1, 2])` → `[2, 1]`
   - `swap(["x", "y"])` → `["y", "x"]`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
