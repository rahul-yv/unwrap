# Exercises: Concurrency

1. Write `fetchAllConcurrently<T>(tasks: Array<() => Promise<T>>): Promise<T[]>` running all the tasks concurrently, returning results in order.

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
