# Exercises: Databases

1. Define `interface UserRow { id: number; name: string }`.
2. Write `getUserByName(db: DatabaseSync, name: string): UserRow | undefined` using a parameterized, prepared statement.

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
