# Exercises: Testing

1. Given `example.ts`'s `add(a: number, b: number): number`, write a `node:test` test checking `add(0, 0) === 0` and `add(-1, 1) === 0`.

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
