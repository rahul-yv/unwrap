# Exercises: OOP

1. Define an interface `Shape` with `area(): number`.
2. Write a `Rectangle` class implementing `Shape`, using constructor parameter properties for `readonly width: number` and `readonly height: number`.
   - `new Rectangle(3, 4).area()` → `12`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
