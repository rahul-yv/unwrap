# Exercises: Collections

1. Write `wordCounts(words: string[]): Map<string, number>` returning a `Map` from each word to its count.
   - `wordCounts(["a", "b", "a"])` → `Map { "a" => 2, "b" => 1 }`

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
