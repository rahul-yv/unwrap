# Exercises: Networking and APIs

1. Write an async function `fetchJson<T>(url: string): Promise<T>` that fetches `url`, throws if `!response.ok`, and returns the parsed JSON body typed as `T`.

Check your answer against [`solutions/exercise_1.ts`](./solutions/exercise_1.ts).
