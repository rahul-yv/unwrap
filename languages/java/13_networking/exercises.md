# Exercises: Networking and APIs

1. Write `String fetchBody(String url) throws IOException, InterruptedException` GETting `url` with a 5-second timeout, throwing a `RuntimeException` if the status isn't 200, otherwise returning the response body.

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
