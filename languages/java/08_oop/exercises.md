# Exercises: OOP

1. Define `record Rectangle(double width, double height)` with a method `area()` returning `width * height`.
   - `new Rectangle(3, 4).area()` → `12.0`
   - `new Rectangle(3, 4).equals(new Rectangle(3, 4))` → `true`

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
