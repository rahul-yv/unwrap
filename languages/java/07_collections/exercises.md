# Exercises: Collections

1. Write `Map<String, Integer> wordCounts(List<String> words)` returning a map from each word to its occurrence count.
   - `wordCounts(List.of("a", "b", "a"))` → `{a=2, b=1}`

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
