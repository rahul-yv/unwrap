# Exercises: Operators

1. Write `int clamp(int value, int low, int high)` restricting `value` to `[low, high]` using `Math.max`/`Math.min`.
   - `clamp(5, 0, 10)` → `5`
   - `clamp(-5, 0, 10)` → `0`
   - `clamp(15, 0, 10)` → `10`

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
