# Exercises: Testing

1. Given `Example.java`'s `add(int a, int b)`, write JUnit `@Test` methods checking `add(0, 0) == 0` and `add(-1, 1) == 0`.

Check your answer against [`solutions/Exercise1Test.java`](./solutions/Exercise1Test.java). Run it the same way as `ExampleTest.java` (see the README), pointing `javac`/`java -cp` at `solutions/`.
