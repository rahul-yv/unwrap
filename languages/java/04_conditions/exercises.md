# Exercises: Conditionals

1. Write `String grade(int score)` returning `"A"` for `score >= 90`, `"B"` for `>= 80`, `"C"` for `>= 70`, `"F"` otherwise, using a `switch` expression with guarded patterns (`case Integer i when ...`).
   - `grade(95)` → `"A"`
   - `grade(72)` → `"C"`
   - `grade(40)` → `"F"`

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
