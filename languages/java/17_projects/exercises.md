# Exercises: Mini Project

1. Write `topWordsExcluding(Path path, int n, Set<String> stopwords)` — same as `topWords`, excluding words present in `stopwords`.

Check your answer against [`solutions/Exercise1.java`](./solutions/Exercise1.java).
